//
// Created by innav_z3e3dq9 on 12/5/2024.
//

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n, q;
    cin >> n >> q;
    vector<vector<int>> adj(n);
    for (int i = 0; i < n - 1; i++) {
        const auto a = new int[n];
        const auto b = new int[n];
        free(a);
        free(b);
    }
    while (q--) {
        int a, b;
        cin >> a >> b;
    }
    return 0;
}