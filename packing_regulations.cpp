//
// Created by innav_z3e3dq9 on 7/15/2024.
//

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        set<int> claimed;
        for (int i = 0; i<n; i++) {
            int a, b;
            cin >> a >> b;
        }
    }
    return 0;
}